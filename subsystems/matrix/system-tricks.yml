id: subsystem.matrix.system-tricks
record_type: subsystem
name: "Matrix System and Decker Tricks"
domain: matrix
status: ingested
effective: {status: current_extension, source: source.matrix, pages: "117-123", basis: extends}

components:
  grid:
    - "comcall tracing"
  host:
    - "bouncer hosts"
    - "chokepoints"
    - "trap doors"
    - "one-way SANs"
    - "dead-end hosts"
    - "vanishing SANs"
    - "virtual machines"
  decker:
    - "improvised cybercombat"
    - "call-forwarding/privacy techniques"
    - "spoofing frame commands"
    - "persona mode switching"
    - "suspending persona operations"

sources:
  - {source: source.matrix, pages: "117-123", relation: extends, note: "System Tricks"}
notes:
  - "These are specialized extensions layered onto existing topology, system-operation and cybercombat rules."
